[tool.commitizen]
tag_format = "v$version"
version = "0.3.0"
version_files = [
    "backend/pyproject.toml:version",
    "backend/libs/cartly-domain/pyproject.toml:version",
    "backend/libs/cartly-infra/pyproject.toml:version",
    "frontend/package.json:version",
]
version_scheme = "pep440"
changelog_file = "CHANGELOG.md"
