[project]
name = "cartly"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "aiosqlite>=0.22.1",
    "cartly-domain",
    "cartly-infra",
    "fastapi[standard]>=0.128.5",
    "pydantic>=2.12.5",
    "pydantic-settings>=2.12.0",
    "structlog>=25.5.0",
]

[dependency-groups]
dev = ["ruff>=0.15.0", "ty>=0.0.15"]
