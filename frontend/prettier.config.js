/**
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */
const config = {
    semi: false,
    singleQuote: true,
    tabWidth: 4,
    useTabs: false,
    printWidth: 100,
    trailingComma: 'all',
}

export default config
